# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*
coverage.xml
htmlcov/
.ipynb_checkpoints/

# Local configuration. Never commit a real one: it is where credentials land.
.env
.env.*
!.env.example
credentials.json
secrets/
*.pem
*.key
*.p12
*.pfx

# Node / reviewer UI (M4)
node_modules/
apps/reviewer/dist/

# Terminology and IG content is licensed and operator-supplied (AGENTS.md 8.3).
# It must never enter the repository, by accident or otherwise.
.fhir/
packages/fhir-cache/
*.tgz
*.jar

# Draft manuscript, its figures, and anything else written about the method.
# Unpublished work product: it stays local until it is published deliberately.
paper/
notebooks/

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
